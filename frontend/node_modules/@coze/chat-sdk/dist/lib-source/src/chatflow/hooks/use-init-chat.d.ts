import { type ChatFrameworkProps } from "../../libs/types";
import { type IChatFlowProps } from '../type';
export declare const useInitChat: (props: IChatFlowProps) => {
    hasReady: boolean;
    chatProps: Omit<ChatFrameworkProps, "auth"> & {
        auth: import("../../libs/types").AuthConf | undefined;
    };
};
