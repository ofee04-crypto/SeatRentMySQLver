import { FC, PropsWithChildren } from 'react';
import { MessageWrapperConfig } from "../../../types/base/ui";
import { ChatMessageGroup, UserInfo } from "../../../types";
export declare const MessageContainer: FC<PropsWithChildren<{
    senderInfo?: UserInfo;
    chatGroup?: ChatMessageGroup;
    isQuery?: boolean;
    className?: string;
    messageWrapperConf?: MessageWrapperConfig;
}>>;
