import { extractChatflowMessage } from './extract-chatlow-message';
export const isSupportAnswerMessage = (item, messageUiMap) => {
    var _a, _b, _c;
    const chatflowNode = extractChatflowMessage(item);
    const contentType = chatflowNode
        ? 'chatflow_node'
        : item.content_type;
    if (item.type !== 'answer') {
        return false;
    }
    if ((_a = messageUiMap === null || messageUiMap === void 0 ? void 0 : messageUiMap[contentType]) === null || _a === void 0 ? void 0 : _a.isSupportMessage) {
        return (_c = (_b = messageUiMap === null || messageUiMap === void 0 ? void 0 : messageUiMap[contentType]) === null || _b === void 0 ? void 0 : _b.isSupportMessage) === null || _c === void 0 ? void 0 : _c.call(_b, item);
    }
    if (!item.content && !item.reasoning_content) {
        return false;
    }
    if (!['object_string', 'text', 'chatflow_node'].includes(contentType)) {
        return false;
    }
    return true;
};
//# sourceMappingURL=is-support-snaswer-message.js.map