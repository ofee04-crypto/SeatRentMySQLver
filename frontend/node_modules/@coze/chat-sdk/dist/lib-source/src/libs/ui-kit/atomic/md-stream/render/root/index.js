import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Fragment } from 'react';
import cls from 'classnames';
import { View } from '@tarojs/components';
import { Phrase, PhraseTypes } from '../phrase';
import { Block, BlockTypes } from '../block';
import './md.css';
import styles from './index.module.less';
export const Root = ({ root, theme = 'light', className, style }) => (_jsx(View, Object.assign({ style: style, className: cls(styles.root, 'md-stream', {
        dark: theme !== 'dark',
    }, className) }, { children: root === null || root === void 0 ? void 0 : root.children.map((item, index) => (_jsxs(Fragment, { children: [BlockTypes.includes(item.type) && (_jsx(Block, { node: item })), PhraseTypes.includes(item.type) && (_jsx(Phrase, { node: item })), item.type === 'tableCell' && null /* 不做处理，做一个标记*/, item.type === 'tableRow' && null /* 不做处理，做一个标记*/, item.type === 'yaml' && null /* 不做处理，做一个标记*/] }, index))) })));
//# sourceMappingURL=index.js.map