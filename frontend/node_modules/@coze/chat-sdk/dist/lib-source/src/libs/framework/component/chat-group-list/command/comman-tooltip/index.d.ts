import { FC, PropsWithChildren } from 'react';
import { ChatMessage } from "../../../../../types";
export declare const CommandTooltip: FC<PropsWithChildren<{
    message: ChatMessage;
    isActive?: boolean;
}>>;
