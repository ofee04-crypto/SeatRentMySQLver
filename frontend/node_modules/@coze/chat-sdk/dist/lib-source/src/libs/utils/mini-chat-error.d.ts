import type { II18n, IMiniChatError } from "../types";
export declare class MiniChatError extends Error implements IMiniChatError {
    readonly code: number;
    readonly msg: string;
    constructor(code: number, msg: string);
    getErrorMessageByI18n(i18n: II18n, options?: Record<string, unknown>, fallbackText?: string): string;
}
export declare const convertToMinChatError: (error: unknown) => MiniChatError;
export declare enum MiniChatErrorCode {
    Unknown = -1,
    Audio_Permission_Denied = 1000,
    Audio_Translation_NoContent = 1001,
    Custom_Conversation_Create_Error = 1002,
    Custom_GetMessageList_Error = 10003,
    SDK_API_OVERDUE_PAYMENT = 4011,
    SDK_API_APP_UnPublished = 4015
}
