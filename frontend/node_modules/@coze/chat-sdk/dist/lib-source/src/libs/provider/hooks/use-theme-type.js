import { useInitBgInfo } from './use-init-bg-info';
export const useThemeType = () => {
    const bgInfo = useInitBgInfo();
    const theme = (bgInfo === null || bgInfo === void 0 ? void 0 : bgInfo.pc) && (bgInfo === null || bgInfo === void 0 ? void 0 : bgInfo.mobile) ? 'bg-theme' : 'light';
    return theme;
};
//# sourceMappingURL=use-theme-type.js.map