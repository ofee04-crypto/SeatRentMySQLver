import { jsx as _jsx } from "react/jsx-runtime";
import { View } from '@tarojs/components';
import { Spacing, Spinning } from "../../../ui-kit";
import { useChatMaxWidth, useChatPropsStore } from "../../../provider";
import styles from './index.module.less';
export const ChatLoading = () => {
    const renderLoading = useChatPropsStore(store => { var _a, _b; return (_b = (_a = store.ui) === null || _a === void 0 ? void 0 : _a.loading) === null || _b === void 0 ? void 0 : _b.renderLoading; });
    const maxWidth = useChatMaxWidth();
    if (renderLoading) {
        return _jsx(View, Object.assign({ className: styles.loading }, { children: renderLoading() }));
    }
    return (_jsx(View, Object.assign({ className: styles.loading, style: {
            maxWidth,
        } }, { children: _jsx(Spacing, Object.assign({ horizontalCenter: true, flex1: true, verticalCenter: true }, { children: _jsx(Spinning, {}) })) })));
};
//# sourceMappingURL=index.js.map