import { createSelectorQuery } from '@tarojs/taro';
import { isArray } from '@tarojs/shared';
export const getViewportBoundingRect = () => new Promise(resolve => {
    var _a, _b;
    (_b = (_a = createSelectorQuery === null || createSelectorQuery === void 0 ? void 0 : createSelectorQuery()) === null || _a === void 0 ? void 0 : _a.selectViewport()) === null || _b === void 0 ? void 0 : _b.boundingClientRect(res => {
        if (!res) {
            return [];
        }
        resolve(isArray(res) ? res : [res]);
    }).exec();
});
//# sourceMappingURL=get-viewport-bounding-rect.js.map