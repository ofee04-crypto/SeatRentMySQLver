import { showToast as showToastTaro } from '@tarojs/taro';
import { UIEventType } from '../types';
export const showToast = (options, targetEventCenter) => {
    if (targetEventCenter) {
        targetEventCenter.trigger(UIEventType.ChatToastShow, options);
    }
    else if (typeof (options === null || options === void 0 ? void 0 : options.content) === 'string') {
        showToastTaro({
            title: options.content,
            icon: options.icon,
            duration: options.duration || 6000,
        });
    }
};
//# sourceMappingURL=toast.js.map