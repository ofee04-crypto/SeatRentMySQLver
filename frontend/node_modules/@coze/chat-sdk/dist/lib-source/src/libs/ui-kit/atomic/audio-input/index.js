import { jsx as _jsx } from "react/jsx-runtime";
import cls from 'classnames';
import { Button } from '@tarojs/components';
import { useAudioRecord } from './use-audio-record';
import styles from './index.module.less';
export const AudioInput = ({ className, onTouching, onEnd, onOutside, children, frameEventTarget, isPcMode, disabled, }) => {
    const { onInitRefForAudioRecord, onTouchStart, onTouchMove, onTouchEnd, onTouchCancel, } = useAudioRecord({
        onTouching,
        onOutside,
        onEnd,
        frameEventTarget,
        isPcMode,
        disabled,
    });
    return (_jsx(Button
    // coze-audio-input-button，用于监听chat组件的keydown方法
    , Object.assign({ 
        // coze-audio-input-button，用于监听chat组件的keydown方法
        className: cls(styles.container, className, 'coze-audio-input-button'), ref: onInitRefForAudioRecord, onTouchStart: onTouchStart, onTouchMove: onTouchMove, onTouchEnd: onTouchEnd, onTouchCancel: onTouchCancel }, { children: children })));
};
//# sourceMappingURL=index.js.map