import { jsx as _jsx } from "react/jsx-runtime";
import classNames from 'classnames';
import { View } from '@tarojs/components';
import { Phrase } from '../../phrase';
import styles from './index.module.less';
export const Table = ({ node }) => (_jsx(View, Object.assign({ className: styles['table-wrapper'] }, { children: _jsx(View, Object.assign({ className: styles.table }, { children: node.children.map((row, rowIndex) => (_jsx(View, Object.assign({ className: classNames(styles['table-row'], {
                [styles['table-header']]: rowIndex === 0,
            }) }, { children: row.children.map((cell, cellIndex) => (_jsx(View, Object.assign({ className: styles['table-cell'] }, { children: cell.children.map((item, index) => (_jsx(Phrase, { node: item }, `row-${rowIndex}-cell-${cellIndex}-content-${index}`))) }), `row-${rowIndex}-cell-${cellIndex}`))) }), `row-${rowIndex}`))) })) })));
//# sourceMappingURL=index.js.map