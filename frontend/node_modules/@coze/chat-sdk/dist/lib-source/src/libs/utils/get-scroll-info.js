import { createSelectorQuery } from '@tarojs/taro';
export const getScrollInfo = (selectorName) => new Promise(resolve => {
    const selector = createSelectorQuery();
    selector
        .select(selectorName)
        .scrollOffset(res => {
        resolve(res);
    })
        .exec();
});
//# sourceMappingURL=get-scroll-info.js.map