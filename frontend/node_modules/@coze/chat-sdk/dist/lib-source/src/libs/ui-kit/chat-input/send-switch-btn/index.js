import { jsx as _jsx, Fragment as _Fragment, jsxs as _jsxs } from "react/jsx-runtime";
import { logger } from "../../../utils";
import { SvgKeyboard, SvgMicrophone } from "../../atomic/svg";
import { InputType } from '../type';
import { SendBtn } from '../send-btn';
import { IconButton } from '../../atomic/icon-button';
import styles from './index.module.less';
export const SendSwitchBtn = ({ inputDisabled, onSendBtnClick, hasTextToSend, inputType, isNeedAudio, onKeyboardClick, onMicrophoneClick, focused, }) => {
    logger.debug('SendSwitchBtn', isNeedAudio);
    const showKeyboard = isNeedAudio && inputType === InputType.Voice;
    const showSendBtn = !isNeedAudio || (inputType === InputType.Text && !!hasTextToSend);
    const showMicrophone = isNeedAudio && inputType === InputType.Text && !hasTextToSend;
    return (_jsxs(_Fragment, { children: [showKeyboard ? (_jsx(IconButton, Object.assign({ onClick: onKeyboardClick, hoverTheme: 'hover', type: "circle-btn", border: "none" }, { children: _jsx(SvgKeyboard, { className: styles['keyboard-icon'] }) }))) : null, showMicrophone ? (_jsx(IconButton, Object.assign({ onClick: onMicrophoneClick, hoverTheme: 'hover', type: "circle-btn", border: "none" }, { children: _jsx(SvgMicrophone, { className: styles['microphone-icon'] }) }))) : null, showSendBtn ? (_jsx(SendBtn, { disabled: inputDisabled || !hasTextToSend, onClick: onSendBtnClick, focused: focused })) : null] }));
};
//# sourceMappingURL=index.js.map