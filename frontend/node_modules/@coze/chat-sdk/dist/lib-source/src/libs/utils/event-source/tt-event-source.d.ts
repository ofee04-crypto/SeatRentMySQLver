import { EventSourceBase } from './event-source';
/**
 * 基于 tt.createEventSource 实现的 EventSource
 * 存在问题： 如果失败返回了json结构，会接收不到消息，直接失败。
 */
export declare class TTEventSource extends EventSourceBase {
    private ttClient?;
    close(): boolean;
    protected _sendMessage(): void;
}
