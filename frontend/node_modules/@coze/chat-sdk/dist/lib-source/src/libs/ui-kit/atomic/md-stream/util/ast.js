export const isParent = (value) => !!(value === null || value === void 0 ? void 0 : value.children);
export const getRegResult = (value, regExps) => {
    let fixMatch = null;
    regExps.find(pattern => {
        fixMatch = pattern.exec(value);
        return fixMatch;
    });
    return fixMatch;
};
//# sourceMappingURL=ast.js.map