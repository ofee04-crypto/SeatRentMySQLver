import { createSelectorQuery } from '@tarojs/taro';
import { isArray } from '@tarojs/shared';
export const getBoundingRect = (selectorName) => new Promise(resolve => {
    var _a, _b;
    (_b = (_a = createSelectorQuery === null || createSelectorQuery === void 0 ? void 0 : createSelectorQuery()) === null || _a === void 0 ? void 0 : _a.select(selectorName)) === null || _b === void 0 ? void 0 : _b.boundingClientRect(res => {
        if (!res) {
            resolve([]);
            return [];
        }
        resolve(isArray(res) ? res : [res]);
    }).exec();
});
//# sourceMappingURL=get-bounding-rect.js.map