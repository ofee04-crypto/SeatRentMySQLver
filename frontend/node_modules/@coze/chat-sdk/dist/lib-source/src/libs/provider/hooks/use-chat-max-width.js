import { logger } from "../../utils";
import { useChatPropsStore } from '../context';
export const useChatMaxWidth = () => {
    const maxWidth = useChatPropsStore(store => {
        var _a, _b, _c, _d, _e, _f;
        logger.debug('ChatSlot useChatPropsStore...', (_c = (_b = (_a = store.ui) === null || _a === void 0 ? void 0 : _a.chatSlot) === null || _b === void 0 ? void 0 : _b.base) === null || _c === void 0 ? void 0 : _c.maxWidth);
        return (_f = (_e = (_d = store.ui) === null || _d === void 0 ? void 0 : _d.chatSlot) === null || _e === void 0 ? void 0 : _e.base) === null || _f === void 0 ? void 0 : _f.maxWidth;
    });
    if (!maxWidth) {
        return undefined;
    }
    const num = Number(maxWidth);
    if (!num) {
        return undefined;
    }
    return Math.max(300, num);
};
//# sourceMappingURL=use-chat-max-width.js.map