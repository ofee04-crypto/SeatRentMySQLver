import { useChatInfoStore, useChatPropsStore } from "..";
export const useInitBgInfo = () => {
    const bgInfoFromProps = useChatPropsStore(store => store.chat.bgInfo);
    const bgInfo = useChatInfoStore(store => { var _a; return (_a = store.info) === null || _a === void 0 ? void 0 : _a.bgInfo; });
    const bgInfoResult = bgInfoFromProps || bgInfo;
    return bgInfoResult;
};
//# sourceMappingURL=use-init-bg-info.js.map