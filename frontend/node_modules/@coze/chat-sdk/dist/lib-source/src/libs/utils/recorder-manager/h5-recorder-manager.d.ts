import { RecorderStartOptions, BaseRecorderManager } from './type';
export declare class H5RecorderManager extends BaseRecorderManager {
    private options;
    private stream;
    private tempFile;
    private size;
    private duration;
    private wavBlob?;
    private isSendCompleted;
    private inputLBuffer;
    private inputRBuffer;
    private audioContext?;
    private audioSource?;
    private analyser?;
    private processor?;
    private getUserMedia;
    start(options: RecorderStartOptions): Promise<void>;
    private startRecord;
    private listenAudioProcess;
    stopMedia(): Promise<void>;
    private flat;
    private genAudioBuffer;
    private _registerVolume;
    private emitStop;
    private emitError;
    destroy(): void;
    stop(): Promise<void>;
    private stopStream;
    pause(): void;
    resume(): void;
}
export declare class H5RecorderManagerInstance extends H5RecorderManager {
    private static curInstance?;
    start(options: RecorderStartOptions): Promise<void>;
}
