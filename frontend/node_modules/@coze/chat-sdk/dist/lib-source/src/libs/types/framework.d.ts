import React from 'react';
import type { AuthConf, UserInfo, ChatInfo, SettingInfo, HeaderConfig, FooterConfig, ChatSlotConfig, IOnImageClickEvent, ErrorUiConfig, LoadingUiConfig, ThemeType, IMessageCallback } from './base';
export interface ChatFrameworkProps {
    chat: ChatInfo;
    auth: AuthConf;
    user: UserInfo;
    ui?: {
        layout?: 'pc' | 'mobile';
        frameworkClassName?: string;
        chatSlotClassName?: string;
        isMiniCustomHeader?: boolean;
        isReadonly?: boolean;
        header?: HeaderConfig;
        footer?: FooterConfig;
        chatSlot?: ChatSlotConfig;
        error?: ErrorUiConfig;
        loading?: LoadingUiConfig;
    };
    setting: SettingInfo;
    children?: React.ReactNode;
    eventCallbacks?: {
        onImageClick?: IOnImageClickEvent;
        onInitRetry?: () => void;
        onLinkClick?: (url: string) => void;
        onThemeChange?: (theme: ThemeType) => void;
        message?: IMessageCallback;
    };
}
