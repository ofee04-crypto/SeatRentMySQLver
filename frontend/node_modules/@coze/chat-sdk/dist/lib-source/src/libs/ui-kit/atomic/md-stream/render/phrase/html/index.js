import { jsx as _jsx, Fragment as _Fragment } from "react/jsx-runtime";
import { memo } from 'react';
import xss from 'xss';
import { RichText } from '@tarojs/components';
import { Text } from '../text';
import { useMdStreamEnableHtmlTags } from '../../../context';
export const Html = memo(({ node }) => {
    const enableHtmlTags = useMdStreamEnableHtmlTags();
    return (_jsx(_Fragment, { children: enableHtmlTags ? (_jsx(RichText, { nodes: `${xss(node.value)}` })) : (_jsx(Text, { node: node })) }));
});
//# sourceMappingURL=index.js.map