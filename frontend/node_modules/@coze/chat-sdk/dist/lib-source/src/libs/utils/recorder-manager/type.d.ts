import Taro from '@tarojs/taro';
import { type TaroStatic } from '@tarojs/taro';
import { AudioRaw } from "../../types";
import { MiniChatError } from '../mini-chat-error';
export declare enum RecorderEvent {
    START = "start",
    PAUSE = "pause",
    RESUME = "resume",
    STOP = "stop",
    ERROR = "error",
    INTERRUPT = "interrupt",
    VOLUME = "volume"
}
export interface RecorderEventArgs {
    [RecorderEvent.START]: unknown;
    [RecorderEvent.PAUSE]: unknown;
    [RecorderEvent.RESUME]: unknown;
    [RecorderEvent.STOP]: AudioRaw;
    [RecorderEvent.ERROR]: MiniChatError;
    [RecorderEvent.INTERRUPT]: unknown;
    [RecorderEvent.VOLUME]: {
        volume: number;
    };
}
export interface RecorderStartOptions {
    sampleRate?: keyof Taro.RecorderManager.SampleRate;
    numberOfChannels?: 1 | 2;
}
export declare abstract class BaseRecorderManager {
    protected event: InstanceType<TaroStatic['Events']>;
    protected isStop: boolean;
    abstract start(options: RecorderStartOptions): Promise<void>;
    abstract pause(): void;
    abstract resume(): void;
    abstract destroy(): void;
    abstract stop(): void;
    emit<T extends RecorderEvent>(event: T, args: RecorderEventArgs[T]): void;
    on<T extends RecorderEvent>(event: T, callback: (args: RecorderEventArgs[T]) => void): void;
    off(event: RecorderEvent, callback: (args: unknown) => void): void;
    isStopped(): boolean;
}
