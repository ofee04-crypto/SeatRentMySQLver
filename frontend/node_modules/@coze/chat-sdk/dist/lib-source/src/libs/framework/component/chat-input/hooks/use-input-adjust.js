var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import { useCallback, useState } from 'react';
import { getBoundingRect, getViewportBoundingRect, logger } from "../../../../utils";
import { useChatPropsStore } from "../../../../provider";
/*
 * When the custom navigation bar is selected for the mini-program,
 * it is necessary to position the location of the <input> element.
 */
export const useInputAdjust = (inputId) => {
    const [bottomOffset, setBottomOffset] = useState(0);
    const isMiniCustomHeader = useChatPropsStore(store => { var _a; return (_a = store.ui) === null || _a === void 0 ? void 0 : _a.isMiniCustomHeader; });
    const inputAdjustDefault = isMiniCustomHeader !== true;
    const changeInputLocation = useCallback((height) => __awaiter(void 0, void 0, void 0, function* () {
        if (inputAdjustDefault) {
            return;
        }
        if (height === 0) {
            setBottomOffset(0);
            return;
        }
        const [viewportRect] = yield getViewportBoundingRect();
        const [inputRect] = yield getBoundingRect(`#${inputId}`);
        const bottomNow = viewportRect.height - inputRect.bottom;
        if (height > bottomNow) {
            setBottomOffset(height - bottomNow);
        }
    }), [inputId]);
    logger.info("Chat Input's inputAdjust value:", {
        inputAdjustDefault,
        isMiniCustomHeader,
    });
    return { changeInputLocation, bottomOffset, inputAdjustDefault };
};
//# sourceMappingURL=use-input-adjust.js.map