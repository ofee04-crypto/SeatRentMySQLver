import { jsx as _jsx } from "react/jsx-runtime";
import { SuggestionList as SuggestionListUi } from "../../../../ui-kit";
import { useSendMessage } from "../../../../services";
export const SuggestionList = ({ messages }) => {
    const { sendTextMessage } = useSendMessage();
    return (_jsx(SuggestionListUi, { suggestions: messages.map(item => item.content), onClickSuggestion: sendTextMessage }));
};
//# sourceMappingURL=index.js.map