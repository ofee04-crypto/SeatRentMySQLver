import type { Parent } from 'mdast';
export declare const isParent: (value?: unknown) => value is Parent;
export declare const getRegResult: (value: string, regExps: RegExp[]) => RegExpExecArray | null;
