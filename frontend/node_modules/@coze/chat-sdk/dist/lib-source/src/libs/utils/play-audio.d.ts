export declare enum AudioPlayEvent {
    STOP = "stop",
    PLAY = "play"
}
export declare class PlayAudio {
    private static instance;
    private isStop;
    private audioContext?;
    private event;
    private audioPrm;
    private filePath;
    static stopNow(): void;
    playText(text: string, audioSpeechFunc: (text: string) => Promise<ArrayBuffer>): Promise<void>;
    playData(data: ArrayBuffer): void;
    play(audioSrc: string): Promise<void>;
    stop(): void;
    removeAudio(): void;
    on(event: AudioPlayEvent, callback: (params: unknown) => void): void;
    emitStopEvent(isError?: boolean): void;
    unregister(): void;
}
