export const getCdnUrl = (baseUrl, urlPath) => {
    let url = '';
    const baseUrlNow = baseUrl ||
        'https://lf3-static.bytednsdoc.com/obj/eden-cn/rkzild_lgvj/ljhwZthlaukjlkulzlp/';
    try {
        return `${(baseUrlNow === null || baseUrlNow === void 0 ? void 0 : baseUrlNow.replace(/\/+$/, '')) || ''}/${urlPath === null || urlPath === void 0 ? void 0 : urlPath.replace(/^\.?\/+/, '')}`;
    }
    catch (err) {
        console.error('Invalid URL:', err, baseUrl, urlPath);
    }
    if (!url) {
        url = '';
    }
    return url;
};
//# sourceMappingURL=get-cdn-url.js.map