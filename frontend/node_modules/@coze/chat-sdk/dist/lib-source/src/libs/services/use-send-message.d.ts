import { type EnterMessage } from '@coze/api';
import { AudioRaw, ChooseFileInfo } from '../types';
import { type RawMessage } from './helper/message';
export declare const useSendMessage: () => {
    sendMessage: (rawMessage: RawMessage, historyMessages?: EnterMessage[] | undefined) => any;
    sendTextMessage: (content: string) => Promise<any>;
    sendFileMessage: (files: ChooseFileInfo[]) => Promise<any>;
    sendAudioMessage: (audio: AudioRaw) => Promise<any>;
    reSendLastErrorMessage: () => Promise<any>;
};
