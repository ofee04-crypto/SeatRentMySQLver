interface ClipboardProps {
    data: string;
    success?: (isUseWeb: boolean) => void;
    fail?: (err: {
        errMsg: string;
    }, isUseWeb: boolean) => void;
}
export declare const setClipboardData: ({ data, success, fail, }: ClipboardProps) => Promise<void>;
export {};
