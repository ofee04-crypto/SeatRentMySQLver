import { type BubbleProps } from '../bubble';
interface BubbleTextProps extends BubbleProps {
    text: string;
    selectable?: boolean;
    space?: boolean;
    enter?: boolean;
    size?: 'large' | 'medium' | 'small';
}
export declare const BubbleText: ({ className, text, size, space, enter, selectable, ...props }: BubbleTextProps) => import("react/jsx-runtime").JSX.Element;
export {};
