import { FC } from 'react';
import { type ChatMessageGroup } from "../../../../types";
export declare const ChatGroup: FC<{
    chatGroup: ChatMessageGroup;
    isProcessing?: boolean;
    isLastMessage?: boolean;
    isShowSuggestion?: boolean;
}>;
