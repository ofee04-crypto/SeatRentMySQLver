import { Logger } from "../../../../utils";
import { type IOnTaskListChange } from "../../../../types";
import { TaskValue } from '../ast';
export declare const useTaskChangeHandle: ({ onTaskChange, logger, }: {
    onTaskChange?: IOnTaskListChange | undefined;
    logger?: Logger | undefined;
}) => {
    onTaskChangeHandle: (taskValue: TaskValue) => any;
};
