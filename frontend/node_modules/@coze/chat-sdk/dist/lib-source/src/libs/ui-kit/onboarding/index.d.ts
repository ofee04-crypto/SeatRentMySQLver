import { FC } from 'react';
import { ChatInfo, IOnImageClickEvent, UserInfo } from "../../types";
interface OnBoardingProps {
    chat?: ChatInfo;
    user?: UserInfo;
    onClickSuggestion?: (text: string) => void;
    onImageClick?: IOnImageClickEvent;
}
export declare const OnBoarding: FC<OnBoardingProps>;
export {};
