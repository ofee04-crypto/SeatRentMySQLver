import type { Root as RootMdType } from 'mdast';
interface SmoothShowMarkdownProps {
    markdown: string;
    isSmooth?: boolean;
    showMarkdownIndex: number;
    enableHtmlTags?: boolean;
    showMoreByte: (byte: number) => void;
}
export declare const useSmoothAstChange: ({ isSmooth, markdown, showMoreByte, showMarkdownIndex, enableHtmlTags, }: SmoothShowMarkdownProps) => {
    onAstChange: (ast: RootMdType) => any;
};
export {};
