import { jsx as _jsx } from "react/jsx-runtime";
import cls from 'classnames';
import { Spacing } from '../atomic/spacing';
import { BubbleText } from '../atomic/bubble-text';
import styles from './index.module.less';
export const SuggestionList = ({ suggestions, onClickSuggestion, isVertical = true, isDisplayAll = false, }) => (_jsx(Spacing, Object.assign({ gap: 8, vertical: isVertical, className: cls(styles['suggestion-list']) }, { children: suggestions
        .slice(0, isDisplayAll ? suggestions.length : 3)
        .map((item, index) => (_jsx(BubbleText, { className: styles.suggestion, text: item, canClick: false, selectable: false, onClick: () => {
            onClickSuggestion === null || onClickSuggestion === void 0 ? void 0 : onClickSuggestion(item);
        } }, index))) })));
//# sourceMappingURL=index.js.map