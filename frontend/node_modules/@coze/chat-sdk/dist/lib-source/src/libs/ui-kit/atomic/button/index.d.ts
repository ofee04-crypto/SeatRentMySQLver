import React from 'react';
export interface BubbleProps {
    className?: string;
    children?: React.ReactNode;
    onClick?: () => void;
}
export declare const Button: ({ className, children, onClick }: BubbleProps) => import("react/jsx-runtime").JSX.Element;
