import { jsx as _jsx, jsxs as _jsxs, Fragment as _Fragment } from "react/jsx-runtime";
import { Fragment, useMemo } from 'react';
import cls from 'classnames';
import { Text as TaroText } from '@tarojs/components';
import { Break } from '../break';
import { useMdStreamSelectable } from '../../../context';
import styles from './index.module.less';
export const Text = ({ node, className }) => {
    const textList = useMemo(() => node.value.split('\n'), [node.value]);
    const selectable = useMdStreamSelectable();
    return (_jsx(_Fragment, { children: textList.map((item, index) => (_jsxs(Fragment, { children: [item ? (_jsx(TaroText, Object.assign({ selectable: selectable, className: cls(styles.text, styles.important, className) }, { children: item }))) : null, index !== textList.length - 1 ? _jsx(Break, {}) : null] }, index))) }));
};
//# sourceMappingURL=index.js.map